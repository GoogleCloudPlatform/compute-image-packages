#!/bin/sh

PREREQS=""
prereqs()
{
	echo "$PREREQS"
}

case $1 in
	prereqs)
		prereqs
		exit 0
		;;
esac

. /usr/share/initramfs-tools/hook-functions

parted=$(command -v "parted")
if [ -n "$parted" ]; then
	copy_exec "$parted" /bin
else
	echo "Something"
fi
